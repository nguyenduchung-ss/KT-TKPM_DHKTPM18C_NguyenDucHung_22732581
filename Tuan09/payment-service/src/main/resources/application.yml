server:
  port: 8084

spring:
  application:
    name: payment-service

  rabbitmq:
    host: 172.16.80.111
    port: 5672
    username: admin
    password: admin123

  cloud:
    openfeign:
      circuitbreaker:
        enabled: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

feign:
  client:
    config:
      default:
        connectTimeout: 5000
        readTimeout: 5000
